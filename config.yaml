# NapCat HTTP API 地址（默认同机部署的 NapCat）
napcat_http_url: "http://127.0.0.1:3000"
# NapCat WebUI token（如启用了 token 认证则填写）
napcat_token: ""
# 请求超时（秒）
request_timeout: 10
# 昵称缓存时间（秒，0 表示不缓存）
nickname_cache_ttl: 300
# 手动昵称映射（格式：QQ号: 昵称）
nickname_override: {}
